import 'package:firebase_core/firebase_core.dart';

// `firebase_options.dart` is produced by `flutterfire configure`. Until you
// run that command the file doesn't exist, so we don't import it directly —
// the conditional import below keeps the build green either way. Once you
// run `flutterfire configure`, replace the body of [currentFirebaseOptions]
// with `return DefaultFirebaseOptions.currentPlatform;` (uncomment the
// import line at the same time).
//
// import '../firebase_options.dart';

FirebaseOptions? currentFirebaseOptions() {
  // After `flutterfire configure`:
  //   return DefaultFirebaseOptions.currentPlatform;
  return null;
}
